Flask>=3.0
pdfplumber>=0.11
openpyxl>=3.1
Werkzeug>=3.0
gunicorn>=21.2
pypdfium2>=4.30
# Email storage posts to a Google Form (-> linked Sheet) using only the stdlib —
# no extra packages needed.
# AI-assisted extraction. Claude (Anthropic) is the preferred engine — it is the
# "A2E" gold standard — with Gemini as a fallback. Both optional at runtime.
anthropic>=0.40
google-genai>=1.0
